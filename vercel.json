{
  "buildCommand": "python -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -r requirements.txt && .venv/bin/mkdocs build",
  "outputDirectory": "site",
  "installCommand": "python -m pip install --upgrade pip --break-system-packages && python -m pip install virtualenv --break-system-packages"
}
